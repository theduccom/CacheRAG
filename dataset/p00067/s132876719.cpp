#include <iostream>
#include <math.h>
#include <algorithm>
using namespace std;

const int mapSize = 12;
char map[mapSize][mapSize];

void dfs(int w, int h)
{
	//?????????????????¨?????????'0'?????????
	map[w][h] = '0';

	//????????????????????????map???????????????????????°????????°????????????????§????
	if (w - 1 >= 0 && map[w - 1][h] == '1') dfs(w - 1, h);			//???
	if (h - 1 >= 0 && map[w][h - 1] == '1') dfs(w, h - 1);			//???
	if (w + 1 < mapSize && map[w + 1][h] == '1') dfs(w + 1, h);		//???
	if (h + 1 < mapSize && map[w][h + 1] == '1') dfs(w, h + 1);		//???
}

int main()
{
	while (true)
	{
		for (int h = 0; h < mapSize; ++h)
		{
			for (int w = 0; w < mapSize; ++w)
			{
				//??\???
				cin >> map[w][h];
				if (cin.eof()) return 0;
			}
		}

		int ans = 0;

		for (int h = 0; h < mapSize; ++h)
		{
			for (int w = 0; w < mapSize; ++w)
			{
				//map[w][h]????????°????????°??°????????????????????????????????????????????£????????????????????????????????????????????????
				if (map[w][h] == '1')
				{
					++ans;
					dfs(w, h);
				}
			}
		}

		cout << ans << endl;
	}
	return 0;
}